# 《飞鸽传书》V1.0 产品规格 ↔ 后端实现 对齐报告

> 对齐基准：`docs/04-product-interaction-spec-v1.0-final.md`（V1.0 产品基线）
> 后端版本：develop @ 4497e5d（含 send-v2 / bind 兜底 / 5分钟保底 / COLS修复）
> 日期：2026-09-01

---

## 一、已实现 ✅（与规格一致）

| 规格条目 | 规格要求 | 后端实现 |
|---|---|---|
| 3.2 自动获得小白 | 首次进入自动创建免费鸽子"小白" 177km/h | ✅ `pigeon/mine` 首次调用自动建小白（IDLE） |
| 4.1 写信内容 | 正文≤500字、一张图、落款 | ✅ send：content(≤500)/imageUrl/signature |
| **4.1 增补** 标题 | 写信可选"写给谁" | ✅ send 新增 title 字段 |
| 4.6 正式放飞 | 服务器时间起飞、正文不可改 | ✅ departure_time=服务器时间，send 后不可改 |
| 5.3 未认领有效期 | 72h 过期、30min 可召回 | ✅ claim_expire=+72h；recall 30min 宽限（RECALL_GRACE_MS） |
| 5.3 召回/过期后分享失效 | 旧分享永久失效 | ✅ RECALLED/UNCLAIMED_EXPIRED 后 share-preview 返回对应错误 |
| 6.3 原子认领 | 单条 UPDATE 保证并发唯一 | ✅ claimLetter 原子 UPDATE（并发安全） |
| 6.3 发件人不能认领 | SENDER_CANNOT_CLAIM | ✅ |
| 6.3 重复请求幂等 | 同收件人重复认领返回当前航程 | ✅ |
| 6.3 不泄露获胜者 | 他人只能看到已认领 | ✅ ALREADY_CLAIMED 不含收件人信息 |
| 7.1 发送即起飞 | FLYING_UNCLAIMED + 不重置起飞时间 | ✅ |
| 7.1/7.2 最短旅程 | required_flight_time = max(distance/177, 5分钟) | ✅ MIN_FLIGHT_HOURS=0.0833（5分钟）保底 |
| 7.3 进度计算 | progress = clamp((now-depart)/(arrival-depart),0,1) | ✅ flight 接口返回 |
| 7.4/7.5/7.6 三种首态 | JUST_DEPARTED(<10%) / ALREADY_FLYING / ARRIVED_WAITING | ✅ firstOpenCase |
| 8.5 本地进度重建 | 返回 departureTime/arrivalTime/serverTime | ✅ flight 返回三时间戳 |
| 9 极简旅途日志 | 起飞/抵达固定记录 + 随机事件、确定性生成、不重复 | ✅ generateEvents（确定性种子+去重） |
| 10 抵达/接信/拆信 | 抵达结算、接信拆信 | ✅ ARRIVED→detail→DELIVERED，抵达时结算 |
| 11 送达回执 | 发件人看状态不看已读 | ✅ flight 只给状态；`read` 字段不对外返回 |
| 12.1/12.2 回信预绑定 | 新收件人=原发件人、后端读取、不分享 | ✅ reply 从原信读 recipient，预绑定 |
| 14.2 真实旅程数据 | 累计里程/送达次数/最远距离 | ✅ recordDelivery 累积 totalMileage/deliveredCount/farthestDistance |
| 17.2 位置最小化 | 只存省市+城市中心点，不泄露精确坐标 | ✅ 兜底用城市中心坐标；接口不返回精确坐标 |
| 22 后端修订清单（核心项） | 发送即起飞/FLYING_UNCLAIMED/72h/30min/5分钟/原子认领/三首态/回信预绑定/不显示已读 | ✅ 全部 |

---

## 二、部分实现 ⚠️（有差异，需确认）

| 规格条目 | 规格要求 | 现状差异 | 建议 |
|---|---|---|---|
| 12.2 回信直接 IN_FLIGHT | 回信不经过 FLYING_UNCLAIMED，直接 IN_FLIGHT | 回信实际 status=FLYING_UNCLAIMED（因收件人已预绑定，无人能抢，行为等价"直达"） | 语义对齐：回信创建时直接置 IN_FLIGHT（小改），或前端按"已预绑定"处理 |
| 13.1/13.2 通知体系 | 发件人+收件人**分别**订阅；回信到达通知 | 当前仅收件人订阅到达（信件级 `subscribed` 单字段），**发件人订阅未实现**，回信到达通知未实现 | 列入 V1.1（需加订阅表/双方字段） |
| 8.2 飞行 Hero 剩余时间 | 剩余时间是唯一首要数字 | flight 返回 progress/剩余时间可计算 ✅，但无"剩余时间"聚合字段 | 前端自行计算，或后端补 remainingMs 字段（可选） |
| 3.1 静默登录 | 后端不能信任裸传 openid，应从服务端登录态解析 | 当前业务接口**直接传 openid 参数 + sign 校验**（sign=md5(openid+secret)，secret 仅后端持有，等效防伪） | 架构上可接受（sign 已保证 openid 不可伪造），如需严格服务端会话再升级 |
| 15 多鸽/购买 | 最多6只、付费购买 | **完全未实现**（见下"未实现"） | 依赖微信虚拟支付资格 |
| 14.1 等级经验 | **V1 明确不做**等级/经验/升级 | ⚠️ **代码已实现等级经验结算**（recordDelivery 计算 exp/level/升级），与规格冲突 | **见第四节冲突项** |

---

## 三、未实现 ❌ → 新版本计划

### P0（核心免费版缺口，建议 V1.1）

| # | 功能 | 规格出处 | 说明 |
|---|---|---|---|
| 1 | **信箱接口（来信/寄出列表）** | 16.5 | 当前无 inbox/sent 列表接口，前端无法做信箱页；需新增 `GET /feige/letter/list?type=inbox|sent` |
| 2 | **往返信件关系（thread_id）** | 12.3 | 无 thread_id/reply_to/root 字段；信箱时间线展示需要 |
| 3 | **到达通知（收件人）真正推送** | 13.1 | subscribed 字段有了，但 FG_ARRIVAL_TEMPLATE_ID 未配，推送未接通（advanceToArrived 已调用 pushSubscribeMessage） |

### P1（完整 V1 范围，V1.2）

| # | 功能 | 规格出处 | 说明 |
|---|---|---|---|
| 4 | 内容安全审核（文字+图片） | 4.5 | 放飞前调微信 msgSecCheck；当前未实现 |
| 5 | 最小投诉入口 | 17.1 | 无 report 接口与表 |
| 6 | 发件人订阅（"到了叫我"）+ 回信到达通知 | 13.1/13.2 | 双方独立订阅模型（当前仅收件人单字段） |
| 7 | 6 只鸽子角色体系 | 14.3 | 当前仅 1 只"小白"，无多鸽创建/选择接口 |
| 8 | 鸽子改名（首达后邀请） | 3.2 | 无改名接口（update-profile 只有用户昵称头像手机号） |
| 9 | 本地草稿 | 4.1 | 前端本地即可，后端不需要（如跨端需要再服务端存） |

### P2（付费能力，V1.3 起，依赖虚拟支付资格）

| # | 功能 | 规格出处 | 说明 |
|---|---|---|---|
| 10 | 多鸽购买（第2-6只付费） | 15 | 订单/支付回调/权益发放幂等/退款；PAID_PIGEON_ENABLED 开关 |
| 11 | 鸽舍扩建与候选角色 | 15.4/15.5 | 需鸽舍管理接口 |

---

## 四、规格冲突项 ⚠️（需产品确认）

| 冲突 | 规格 | 现状 | 影响 |
|---|---|---|---|
| **等级/经验** | 14.1「V1不计算或展示 Level/EXP/升级/速度提升」 | 代码已实现：`settle_exp_delta / settle_level_before/after / settle_level_up` 字段 + recordDelivery 结算逻辑，detail 返回 settleLevelUp | 若严格按规格，应**关闭结算**（只记里程不记经验）；若产品接受轻量成长，则规格需更新。**当前已在跑，会产生经验数据** |

---

## 五、前端可见的接口缺口汇总

| 前端页面（规格 16/18） | 所需后端接口 | 现状 |
|---|---|---|
| 鸽舍首页（多鸽+汇总） | 鸽子列表 `GET /pigeon/list` | ❌ 仅 `pigeon/mine` 单只 |
| 信箱-来信 | 来信列表 | ❌ 无 |
| 信箱-寄出 | 寄出列表 | ❌ 无 |
| 单鸽详情/旅程履历 | 鸽子旅程列表 | ❌ 无 |
| 投诉页 | `POST /report` | ❌ 无 |
| 鸽舍空位购买 | 购买/订单接口 | ❌ 无 |
| 首次发送前简化首页 | `isSendLetter`（登录已返回） | ✅ 已有 |
| 到达通知深链 | 通知订阅（收件人） | ⚠️ 字段有、推送未配模板 |
| 内容审核失败提示 | 审核接口/回调 | ❌ 无 |

---

## 六、建议路线

1. **V1.0 收尾（当前）**：补信箱列表 + thread_id（前端信箱/时间线必备，量小）
2. **V1.1**：内容审核、投诉、双端通知订阅、6只鸽子+改名
3. **V1.2**：付费购买（等虚拟支付资格）、等级经验冲突项决议
